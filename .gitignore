# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

**/.DS_Store
*.crt
*.key
*.ISO
*.img
cache
build.conf

sedunlocksrv/sedunlocksrv

# Ignore Resilio Sync selective sync placeholders
*.rsls*

# SSH build temporary files
ssh/dropbear*
ssh/authorized_keys

# Examples directory (local scripts and configs)
examples/