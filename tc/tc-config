#!/bin/busybox ash

# Custom Tiny Core Linux RC Script

# Hide cursor
echo -e "\e[?1c"
# Disable showing keyboard input
stty -echo
# Disable input buffering
stty cbreak min 1

# ANSI COLORS
GREEN="$(echo -e '\033[1;32m')"
MAGENTA="$(echo -e '\033[1;35m')"
NORMAL="$(echo -e '\033[0;39m')"

export PATH=/usr/local/sbin:/usr/local/bin:"$PATH"

# Start Udev to populate /dev and handle hotplug events
/sbin/udevd --daemon 2>&1 >/dev/null
/sbin/udevadm trigger --action=add 2>&1 >/dev/null &

# Setup clock
while [ ! -e /dev/rtc0 ]; do usleep 50000; done
/sbin/hwclock -u -s &

/sbin/ifconfig lo 127.0.0.1 up
/sbin/route add 127.0.0.1 lo &

# Creates the necessary links and cache to the most recent shared libraries
/sbin/ldconfig 2>/dev/null

# --- LACP (802.3ad) via sysfs  ---

# requires the inclusion of the ipv6_netfilter package which includes the bonding driver
modprobe bonding

# 1. Create the bond0 interface via bonding_masters
echo +bond0 > /sys/class/net/bonding_masters

# 2. Configure LACP parameters via sysfs before bringing it up
echo 4 > /sys/class/net/bond0/bonding/mode
echo 1 > /sys/class/net/bond0/bonding/miimon
echo 1 > /sys/class/net/bond0/bonding/lacp_rate
echo 1 > /sys/class/net/bond0/bonding/xmit_hash_policy

# 3. Enslave physical interfaces
for iface in /sys/class/net/e*; do
    IFNAME=$(basename "$iface")
    [ "$IFNAME" == "bond0" ] && continue
    ip link set "$IFNAME" down
    echo "+$IFNAME" > /sys/class/net/bond0/bonding/slaves
done

# 4. Configure Networking using BusyBox's built-in commands
ip addr add 192.168.10.230/24 dev bond0
ip link set bond0 up
ip route add default via 192.168.10.253 dev bond0
echo "nameserver 192.168.10.252" > /etc/resolv.conf

# 5. Ping Test (Gateway Check)
echo "Waiting 10s for LACP negotiation..."
sleep 10
if ping -c 3 -W 5 192.168.10.253 > /dev/null; then
    echo "Network OK: Gateway reachable."
else
    echo "NETWORK ERROR: Gateway unreachable!"
    # Debug: Show bond status from the kernel
    cat /proc/net/bonding/bond0
fi

# Fix to get the 64-bit binaries working
if [ ! -d /lib64 ]; then
    ln -s /lib /lib64
fi

if [ -e /home/tc/keymap ]; then
    KEYMAP=$(cat /home/tc/keymap)
    keymapfile=$(find /usr/share/kmap/ -name "${KEYMAP}.kmap")
    echo "Loading keymapfile $keymapfile"
    loadkmap < $keymapfile
fi

sedutil-cli --scan

cd /usr/local/sbin/sedunlocksrv/
./sedunlocksrv

# Reboot when sedunlocksrv exits
echo "Rebooting..."
sleep 3
reboot -nf
